{
  "name": "heroku-auth-proxy",
  "version": "1.0.0",
  "description": "A proxy for the Heroku Auth API to be used with client side apps",
  "main": "server.js",
  "author": "Steve Heyes",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.15.1",
    "config": "^1.20.1",
    "express": "^4.13.4",
    "morgan": "^1.7.0"
  },
  "devDependencies": {
    "jest": "^20.0.4",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "start": "NODE_ENV=dev && node server.js",
    "test": "jest"
  }
}
